#include <switch.h>
#define GameName
#define paste
class NewGameFirmware {
	if (this.GameName.update == "Mario Kart 8 Deluxe") {
		this.paste[] = paste.this("Current Game Version");
		else = this.paste[]NULL;
	
	}
	else if (this.GameName.update == false) {
		this.get[CurrentGameVersion() = true];
		return CurrentGameVersion();
		return 0;
	}
};
