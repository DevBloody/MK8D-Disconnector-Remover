#include <iostream>
#include <switch.h>
#include "MK8_NewerFirmware_Supporter.hpp"
#include <stdlib.h>
#include <stdio.h>
#include "BRITISH.h"
#include "CHINESE.h"
#include "ENGLISH.h"
#include "FRENCH.h"
#include "JAPANESE.h"
#include "KOREAN.h"
#include "PORTUGUESE.h"
#include "SPANISH.h"
#define Color
#define UI
class Update {
	if (this.app == [new("Update")]) {
		return app;
		this.UI = this.BackColor(Color.White);
		this.UI = this.add[progressbar];
		this.progressbar = this.progressbar.BackColor(Color.Gray);
		this.progressbar = this.progressbar.LoaderColor(Color.Blue);
		this.UI = this.add[timer];
		this.starting.timer = "99:99:99";
		if (timer.start == true) {
			if (this.used.language == true) {
				this.printf(british);
				this.printf(chinese);
				this.printf(english);
				this.printf(french);
				this.printf(japanese);
				this.printf(korean);
				this.printf(portuguese);
				this.printf(spanish);
			}
		}
	}
};